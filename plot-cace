#!/usr/bin/env bash
set -euo pipefail
root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ $# -eq 0 ]]; then
    set -- plot
fi
exec distrobox enter iic-osic-tools2 -- python3 "$root/scripts/plot_cace.py" --root "$root" "$@"
